name: Test Trigger

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Teste simples
        run: |
          echo "🚀 Workflow executado com sucesso!"
          echo "Data:"
          date
